{
  "name": "vistaiq",
  "version": "1.0.0",
  "description": "VistaIQ — AI Decision Intelligence Platform for Tourism & Local Economic Development",
  "private": true,
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "sql.js": "^1.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
